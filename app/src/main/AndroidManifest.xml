<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.tupt.torqapp">

    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".Activity.SampleActivity"></activity>
        <activity android:name=".Activity.BudgetActivity" />
        <activity android:name=".Activity.RequestActivity" />
        <activity android:name=".Activity.ProfileActivity" />
        <activity android:name=".Activity.List.PpmpActivity" />
        <activity android:name=".Activity.HomeActivity" />
        <activity
            android:name=".Activity.MainActivity"
            android:theme="@style/AppTheme.NoActionBar" /> <!-- <activity android:name=".Activity.TracklogsActivity"></activity> -->
        <!-- <activity android:name=".Activity.BudgetActivity" /> -->
        <!-- <activity android:name=".Activity.CourseBudget" /> -->
        <!-- <activity android:name=".Activity.AppActivity" /> -->
        <!-- <activity android:name=".Activity.PpmpActivity" /> -->
        <!-- <activity android:name=".Activity.ProfileActivity" /> -->
        <activity
            android:name=".Activity.LoginActivity"
            android:theme="@style/AppThemeLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>